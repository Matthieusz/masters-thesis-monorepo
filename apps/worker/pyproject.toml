[project]
name = "quantum-routing-worker"
version = "0.1.0"
description = "Background worker for the quantum routing platform"
requires-python = ">=3.14,<4.0"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
