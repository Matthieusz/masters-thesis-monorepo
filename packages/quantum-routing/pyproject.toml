[project]
name = "quantum-routing"
version = "0.1.0"
description = "Quantum routing algorithms and evaluation tools"
requires-python = ">=3.14,<4.0"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
